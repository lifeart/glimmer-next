import { Component } from '@lifeart/gxt';

export default class LoadMeAsync extends Component {
  <template>
    <div class='inline-flex flex-col items-center'>Async component "{{@name}}"</div>
  </template>
}
